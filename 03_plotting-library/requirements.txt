# Python dependencies for existing Python templates.
# Static scientific figures should prefer R templates when available.
matplotlib>=3.7
numpy>=1.24
pandas>=2.0
seaborn>=0.12
scipy>=1.10
plotly>=5.15
networkx>=3.0
lifelines>=0.27

# R-side packages are installed through R, not pip:
# ggplot2, ggrepel, patchwork, cowplot, ComplexHeatmap, circlize, survival,
# survminer, forestplot, maftools, ggpubr, ggsci, viridis
